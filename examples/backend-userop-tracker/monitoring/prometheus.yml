# Scrape the example's exporter. It runs on the host, so from inside the
# Prometheus container the host is reachable at host.docker.internal.
global:
  scrape_interval: 5s

scrape_configs:
  - job_name: loom
    static_configs:
      - targets:
          - "host.docker.internal:9464"
